Namespace Global.Mathematics   
   public Class Calculator
        'Procedure to add 2 numbers
        ' Sub Addition(a as Integer, b as Integer)
        '     Dim c as Integer = a+b 'Expression to compute addition
        '     Console.Write($"{a} + {b} = {c}") 'printing the result to the user
        ' End Sub  

        'Function - is a procedure which returns a value as output
        Function Addition(a as Integer, b as Integer) As Integer
            return a+b
        End Function
        
    End Class  
End Namespace 
